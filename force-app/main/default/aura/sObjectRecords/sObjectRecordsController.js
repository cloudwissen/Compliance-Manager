({
	/**
	* @description Method to be called during component initalisation to get the records list from the map
	*/
	getRecords : function(component,event,helper){
		helper.getRecords(component);
	},
})